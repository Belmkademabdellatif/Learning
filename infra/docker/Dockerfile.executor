FROM docker:24-dind

# Install Node.js for the executor service
RUN apk add --no-cache nodejs npm python3 py3-pip

# Pre-pull execution images
RUN docker pull node:18-alpine && \
    docker pull python:3.11-alpine

WORKDIR /executor

# Keep container running
CMD ["dockerd"]
